# 面试题

## 程序运行题

```js
var length = 10;
function fn() {
    console.log(this.length)
}
var obj = {
    length: 5,
    method: function(fn) {
        fn();
        //也就是fn(),但是这个this指向arguments，因为是arguments的0调用了这个函数
        arguments[0]();
    }
}
obj.method(fn,1)
```
